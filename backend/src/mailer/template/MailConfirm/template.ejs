<!DOCTYPE html>
<html lang="zh">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title><% if (title) { %> <%= title %> <% } %></title>
  </head>
  <body>
    <table style="width: 100%; height: 100%">
      <tbody>
        <tr>
          <% if (css) { %> <%- css %> <% } %>
          <td>
            <div class="mail__container">
              <!-- 订阅者名称 -->
              <% if (userName) { %>
              <div class="mail__container__subscriber">
                你好呀~ <span class="text-emphasize"><%= userName %></span>
              </div>
              <% } %>
              <!-- 订阅者邮箱 -->
              <% if (email) { %>
              <div class="mail__container__captcha">
                <div class="mail__container__captcha-tips">
                  以下是你在
                  <a href="<%= blogConfig.publicPath %> ">
                    <% if (blogConfig.blogName) { %> <%= blogConfig.blogName %>
                    <% } else { %> 某博客 <% } %>
                  </a>
                  注册的邮箱验证码：
                </div>
                <div class="mail__container__captcha-text">
                  <span class="text-emphasize"><%= captcha %></span>
                </div>
              </div>
              <% } %>
              <div class="footer">
                <div class="footer__left">
                  <div class="footer__left-text">
                    若不是你订阅的邮件，请勿理会<br />
                    本邮件由系统发出，请勿回复
                  </div>
                </div>
                <div class="footer__right">
                  <div class="footer__right__avatar">
                    <% if (blogConfig.avatarUrl) { %>
                    <a href="<%=blogConfig.publicPath %>"
                      ><img
                        class="footer__right__avatar-img"
                        src="<%= blogConfig.avatarUrl %>"
                    /></a>
                    <% } %>
                  </div>
                  <div class="footer__right__time">
                    <% if (blogConfig.blogger) { %> <%= blogConfig.blogger %><br />
                    <% } %> <%= time %>
                  </div>
                </div>
                <div class="clear-both"></div>
              </div>
            </div>
          </td>
        </tr>
      </tbody>
    </table>
  </body>
</html>
